import express from "express";import u from "@/utils";import{success}from "@/lib/responseFormat";import{getImages}from "@/services/assetWorkspace";import{send,userId}from "@/services/assetWorkspace/http";import{requireProjectResourceAccess}from "@/services/team/authorization";
export default express.Router().post("/",async(req,res)=>{try{await requireProjectResourceAccess(u.db,userId(req),{table:"o_assets",id:req.body?.assetsId},"read");const out=await getImages(u.db,Number(req.body.assetsId));out.tempAssets=await Promise.all(out.tempAssets.map(async (x:any)=>({...x,filePath:x.filePath?await u.oss.getSmallImageUrl(x.filePath):""})));return res.send(success(out))}catch(e){return send(res,e)}})
