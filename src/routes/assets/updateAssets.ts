import express from "express";import u from "@/utils";import{success}from "@/lib/responseFormat";import{updateAsset}from "@/services/assetWorkspace";import{actor,send,userId}from "@/services/assetWorkspace/http";import{requireProjectAccess}from "@/services/team";
export default express.Router().post("/",async(req,res)=>{try{await requireProjectAccess(u.db,userId(req),req.body?.projectId,"edit");return res.send(success(await updateAsset(u.db,req.body,actor(req))))}catch(e){return send(res,e)}})
